<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- Default landing page → redirects to login via AuthFilter -->
    <welcome-file-list>
        <welcome-file>login</welcome-file>
    </welcome-file-list>

    <!-- ── Custom Error Pages ───────────────────────────────────── -->
    <error-page>
        <error-code>404</error-code>
        <location>/WEB-INF/pages/error404.jsp</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/WEB-INF/pages/error500.jsp</location>
    </error-page>

    <!--
        Existing servlets use @WebServlet annotations (no entry needed).
        AboutServlet and ContactServlet are explicitly registered here
        to guarantee Tomcat picks them up without relying on annotation scanning.
    -->

    <!-- ── AboutServlet ─────────────────────────────────────────── -->
    <servlet>
        <servlet-name>AboutServlet</servlet-name>
        <servlet-class>com.myMusicList.controllers.AboutServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>AboutServlet</servlet-name>
        <url-pattern>/about</url-pattern>
    </servlet-mapping>

    <!-- ── ContactServlet ───────────────────────────────────────── -->
    <servlet>
        <servlet-name>ContactServlet</servlet-name>
        <servlet-class>com.myMusicList.controllers.ContactServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>ContactServlet</servlet-name>
        <url-pattern>/contact</url-pattern>
    </servlet-mapping>

</web-app>